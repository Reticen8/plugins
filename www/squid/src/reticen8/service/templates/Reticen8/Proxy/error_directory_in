{#
    base64 encoded zip archive containing template overrides
#}
{
  "install": "{{ Reticen8.proxy.general.error_pages|default('reticen8') }}",
  "content": "{% if not helpers.empty('Reticen8.proxy.error_pages.template') %}{{ Reticen8.proxy.error_pages.template }}{% endif %}"
}
