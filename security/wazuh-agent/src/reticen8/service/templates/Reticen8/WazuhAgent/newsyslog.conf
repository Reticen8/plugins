# logfilename			                [owner:group]	mode	count	size	when	flags	[/pid_file]				[sig_num]
{% if not helpers.empty('Reticen8.WazuhAgent.general.enabled') %}
/var/ossec/logs/reticen8_syslog.log     root:wazuh      660     2       *       $D0     Z       /var/run/syslog-ng.pid
{% endif %}
